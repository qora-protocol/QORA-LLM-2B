[package]
name = "qor2b"
version = "0.1.0"
edition = "2024"

[dependencies]
half = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokenizers = { version = "0.22", default-features = false, features = ["onig"] }
rayon = "1"
safetensors = "0.4"
